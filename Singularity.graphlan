Bootstrap: docker
From: continuumio/miniconda


%files
	graphlan.yaml

%post
        ### Installs graphlan, hclust2 and export2graphlan to conda environment "graphlan", then activates it.

        PATH=/opt/conda/bin:$PATH
        export PATH

	conda env create -f graphlan.yaml --force

%runscript
        echo "$@"



